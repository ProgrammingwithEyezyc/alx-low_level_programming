#include <stdio.h>
/**
 * main - all the codes inside the main function will execute
 *
 * Return: returning 0
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
