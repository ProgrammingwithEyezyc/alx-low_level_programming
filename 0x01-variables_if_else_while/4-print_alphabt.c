#include <stdio.h>
/**
 * main - code goes in here
 * Description: get a random number
 * Return: returning 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		if (c != 'e' && c != 'q')
		{
			putchar(c);
		}
		c++;
	}
	putchar('\n');
	return (0);
}
