#include "main.h"
/**
 * add - This add two integers
 * @one: first number input
 * @two: second number input
 * Return: return the sum of both input
 */
int add(int one, int two)
{
	int sum;

	sum = one + two;
	return (sum);
}
